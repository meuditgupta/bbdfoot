<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bbdfootprint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".guide"></activity>
        <activity android:name=".noticescr" />
        <activity android:name=".feedsc" />
        <activity android:name=".hact" />
        <activity android:name=".nitact" />
        <activity android:name=".canact" />
        <activity android:name=".uniact" />
        <activity android:name=".staact" />
        <activity android:name=".new3" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>